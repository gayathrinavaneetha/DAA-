"""Importable, unit-testable implementations of the DAA lab algorithms.

The repository root contains the original coursework scripts as loose files
(no ``.py`` extension, prose headers, and module-level demo/``print`` drivers),
which makes them impossible to ``import`` or measure for coverage. This package
re-exposes the same algorithms as clean, importable functions so they can be
exercised by the pytest suite in ``tests/``.
"""
